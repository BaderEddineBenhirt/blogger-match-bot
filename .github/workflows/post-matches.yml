name: Post Matches to Blogger
on:
  schedule:
    - cron: '0 1 * * *'  
  workflow_dispatch:     

jobs:
  post-matches:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install axios cheerio
        
      - name: Check Environment Variables
        run: |
          echo "Checking for environment variable availability..."
          echo "BLOG_ID is set: ${{ secrets.BLOG_ID != '' }}"
          echo "CLIENT_ID is set: ${{ secrets.CLIENT_ID != '' }}"
          echo "CLIENT_SECRET is set: ${{ secrets.CLIENT_SECRET != '' }}"
          echo "REFRESH_TOKEN is set: ${{ secrets.REFRESH_TOKEN != '' }}"
        
      - name: Run match posting script
        run: node match-bot.js
        env:
          BLOG_ID: ${{ secrets.BLOG_ID }}
          API_KEY: ${{ secrets.API_KEY }}
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}


